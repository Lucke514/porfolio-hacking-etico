---
import '@/styles/globals.css';
import Layout from '../layouts/Layout.astro';
import { Shield, Key, Activity } from 'lucide-react';
import { ToolCard } from '@/components/maintain-access/ToolCard';
import { SecurityPolicyCard } from '@/components/maintain-access/SecurityPolicyCard';
import { herramientas, politicasSeguridad, videoURL } from '@/components/maintain-access/data';
---

<section class="min-h-screen py-20">
    <div class="container mx-auto px-4">
        <!-- Header Section -->
        <div class="relative max-w-4xl mx-auto text-center mb-16">
            <div class="absolute inset-0 bg-gradient-to-r from-purple-200 to-pink-200 dark:from-purple-900/20 dark:to-pink-900/20 rounded-3xl blur-3xl opacity-30"></div>
            <div class="relative p-8 ">
                <h1 class="text-5xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-purple-600 to-pink-600 dark:from-purple-400 dark:to-pink-400 mb-6">
                    Fase 4: Mantener Acceso
                </h1>
                <p class="text-xl text-gray-700 dark:text-gray-300 leading-relaxed">
                    En esta etapa, se busca establecer una presencia persistente en el sistema objetivo, permitiendo movimientos laterales, escalada de privilegios y exfiltración de datos sin ser detectados.
                </p>
            </div>
        </div>

        <!-- Herramientas Section -->
        <div class="max-w-7xl mx-auto mb-20">
            <div class="flex items-center justify-center gap-4 mb-10">
                <Key class:list="w-8 h-8 text-purple-600 dark:text-purple-400" />
                <h2 class="text-3xl font-bold text-gray-900 dark:text-gray-100">
                    Herramientas para Mantener Acceso
                </h2>
            </div>
            <div class="grid gap-8 md:grid-cols-2">
                {herramientas.map((herramienta) => (
                    <ToolCard tool={herramienta} />
                ))}
            </div>
        </div>

        <!-- Políticas de Seguridad Section -->
        <div class="max-w-7xl mx-auto mb-20">
            <div class="flex items-center justify-center gap-4 mb-10">
                <Shield class:list="w-8 h-8 text-purple-600 dark:text-purple-400" />
                <h2 class="text-3xl font-bold text-gray-900 dark:text-gray-100">
                    Políticas de Seguridad
                </h2>
            </div>
            <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
                {politicasSeguridad.map((politica) => (
                    <SecurityPolicyCard policy={politica} />
                ))}
            </div>
        </div>

        <!-- Video Section -->
        <div class="max-w-4xl mx-auto">
            <div class="flex items-center justify-center gap-4 mb-10">
                <Activity class:list="w-8 h-8 text-purple-600 dark:text-purple-400" />
                <h2 class="text-3xl font-bold text-gray-900 dark:text-gray-100">
                    Video Explicativo
                </h2>
            </div>
            <div class="relative aspect-video rounded-xl overflow-hidden shadow-xl">
                <iframe
                    src={videoURL}
                    title="Video sobre Control de Seguridad en Aplicaciones"
                    class="absolute inset-0 w-full h-full"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen
                ></iframe>
            </div>
        </div>
    </div>
</section>