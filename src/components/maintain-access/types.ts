export interface Tool {
    nombre: string;
    descripcion: string;
    url: string;
    requerimientos: string;
    icon: string;
}

export interface SecurityPolicy {
    titulo: string;
    descripcion: string;
    icon: string;
}